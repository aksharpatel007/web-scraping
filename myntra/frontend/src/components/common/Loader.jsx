const Loader = () => (
    <div className="flex min-h-[40vh] items-center justify-center">
        <div className="glass-panel rounded-full px-6 py-3 text-sm text-mist">Loading premium experience...</div>
    </div>
);

export default Loader;
